<h1><%=@song.name%></h1>
<p><%=link_to 'Edit', edit_song_path(@song)%></p>
<p><%=link_to 'My Genre', genre_path(@song.genre.id)%></p>
<p><%=link_to @song.artist.name, artist_path(@song.artist.id)%></p>
<h3><%=link_to 'All Songs', songs_path%></h3>